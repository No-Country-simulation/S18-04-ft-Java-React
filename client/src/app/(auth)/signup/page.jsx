import { SignupForm } from '@/components';

// Este componente se vera en la ruta /signup (Nombre de la carpeta)
const SignUpPage = () => {
  return (
    <div>
      <main >
        <SignupForm />
      </main>
    </div>
  );
};

export default SignUpPage;
